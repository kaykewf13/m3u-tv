// Barrel file for TypeScript resolution — Metro uses .native.tsx or .web.tsx at runtime.
export { FocusablePressable } from './FocusablePressable.web';
export type { FocusablePressableProps, FocusablePressableRef } from './FocusablePressable.types';
